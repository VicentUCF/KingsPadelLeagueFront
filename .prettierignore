.astro/
.vscode/
dist/
node_modules/
coverage/
playwright-report/
test-results/
.vercel/

# Generado por `tinacms dev`/`build` (tina-lock.json es un lockfile de esquema, se commitea tal
# cual; __generated__/ son artefactos de build, ver .gitignore).
tina/tina-lock.json
tina/__generated__/

# Not web app source: a separate Blender asset project vendored in-repo.
design/

# Edited via Decap CMS by non-technical writers (see README "Gestor de
# noticias"); reformatting their Markdown style creates noise unrelated to
# their edits.
src/content/
